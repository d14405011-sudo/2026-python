# TEST_CASES

## UVA 10226
- 固定案例：`N=3` 且無禁位，驗證 6 組排列與壓縮輸出格式。
- 邊界案例：`N=2` 且 `A` 禁止站位 1，驗證只剩唯一合法答案。

## UVA 10235
- 小型暴力對拍：使用 1x1、2x2、與含插座的 2x2 格局。
- 驗證主程式與 easy 版輸出皆與暴力枚舉一致。

## UVA 10242
- 固定案例：使用 ATM 題常見範例圖，預期最大掠奪金額為 47。
- 驗證主程式與 easy 版輸出一致。

## UVA 10252
- 奇數點案例：中位數唯一。
- 偶數點案例：中位數區間非單點，驗證解個數計算正確。

## UVA 10268
- 一般案例：小樓層、常見參數。
- 壓力案例：超大樓層，驗證超過 63 次時輸出固定訊息。
